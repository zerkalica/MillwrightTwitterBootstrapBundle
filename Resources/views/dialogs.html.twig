{% macro dialog_modal(id, header, body, footer) %}
<div class="modal" id="{{ id }}">
    <div class="modal-header">
        <a class="close" data-dismiss="modal">×</a>
        <h3>{{ header }}</h3>
    </div>
    <div class="modal-body">
        {{ body }}
    </div>
    <div class="modal-footer">
        {{ footer }}
    </div>
</div> <!-- /modal -->
{% endmacro dialog_modal %}
